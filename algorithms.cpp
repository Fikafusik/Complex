
#include "algorithms.hpp"

/*
void sort_stupid(int a[], int l, int r) {
    for (int i = l + 1; i <= r; i++)
        for (int j = i; j > l; j--)
            if (a[j-1] < a[j]) swap(a[j-1], a[j]);
}*/